# Tauri boundary

The native shell will own Windows file dialogs, secure local storage, FFmpeg process coordination, and Rust command invocation. Keep model credentials and raw media out of the webview.

